//import {fetchLatestInvoices} from "@/app/lib/data";

export default async function PortalIngreso() {
    //const latestInvoices = await fetchLatestInvoices();
  
    return (
      <div className="flex flex-col gap-4 items-center justify-center min-h-screen p-8 pb-20 sm:p-20 font-[family-name:var(--font-geist-sans)]">
        <h1 className="text-2xl font-bold">Portal de Ingreso</h1>
        <p className="text-lg">Aqui va el portal de ingreso.</p>
      </div>
    );
  }